# App

The application source code. Consumes library.

## Documentation

The app works as an application, imports the simple mathematics library and uses it to implement the intersection algorithm.

We have no unit tests for the application. The reason being, i used desmos to visually set the input values, and manually confirm numerical results. The reason being, i spent too much time implementing everything else so far :(
