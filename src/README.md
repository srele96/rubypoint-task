# Libary implementation

The source code of library implementation.

## Documentation

Currently as placeholder because current library is generic and can not be split into `.h` and `.cpp` files.

The library is meant to be compiled and statically linked.
